name: LunarCalendarBar
options:
  bundleIdPrefix: com.lunarcalendar
  deploymentTarget:
    macOS: "14.0"

packages:
  LunarCalendarBar:
    path: .
    product: LunarCalendarBar

targets:
  LunarCalendarBar:
    type: application
    platform: macOS
    sources:
      - LunarCalendarBar
      - path: LunarCalendarBar/Resources/Info.plist
        buildPhase: none
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.lunarcalendar.bar
        PRODUCT_NAME: LunarCalendarBar
        INFOPLIST_FILE: LunarCalendarBar/Resources/Info.plist
        GENERATE_INFOPLIST_FILE: NO
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        COMBINE_HIDPI_IMAGES: YES
        CODE_SIGN_IDENTITY: "-"
        CODE_SIGNING_REQUIRED: NO
        CODE_SIGNING_ALLOWED: NO
        ENABLE_HARDENED_RUNTIME: NO
        SWIFT_VERSION: "5.9"
        MACOSX_DEPLOYMENT_TARGET: "14.0"